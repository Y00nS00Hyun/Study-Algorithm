#include <stdio.h>
int main()
{
    int n, m, i, arr[100], sw[200];
    scanf("%d %d", &n, &m);

    for (i = 0; i < 2 * m; i += 2)
    {
        scanf("%d %d", &sw[i], &sw[i + 1]);
    }

    for (i = 0; i < 2 * m; i += 2)
    {
    }
    return 0;
}