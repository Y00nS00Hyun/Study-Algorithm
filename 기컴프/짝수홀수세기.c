#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
    int arr[100];
    int odd_count = 0;
    int even_count = 0;

    srand(time(NULL));
    for (int i = 0; i < 100; i++)
    {
        printf("");
    }
}
